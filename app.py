import re # เพิ่มการนำเข้า re สำหรับตรวจสอบภาษาอังกฤษ

# --- ฟังก์ชันตรวจสอบความถูกต้องของ UserID ---
def is_valid_userid(user_id):
    # 1. เช็กความยาว > 6
    if len(user_id) <= 6:
        return False, "UserID ต้องมีความยาวมากกว่า 6 ตัวอักษร"
    
    # 2. เช็กว่าเป็นภาษาอังกฤษหรือตัวเลขเท่านั้น (ห้ามเว้นวรรค/ภาษาไทย)
    if not user_id.isalnum():
        return False, "UserID ต้องเป็นภาษาอังกฤษหรือตัวเลขเท่านั้น (ห้ามมีสัญลักษณ์หรือเว้นวรรค)"
    
    # 3. เช็กว่าเป็นภาษาอังกฤษ (ป้องกันการพิมพ์เลขไทยหรืออักขระพิเศษอื่นๆ)
    if not re.match("^[a-zA-Z0-9]*$", user_id):
        return False, "UserID รองรับเฉพาะตัวอักษรภาษาอังกฤษ A-Z และตัวเลข 0-9 เท่านั้น"
    
    return True, ""

# --- ฟังก์ชันสมัครสมาชิกที่เพิ่มการเช็ก ID ซ้ำ ---
def sign_up(user_id, password, full_name, student_id):
    # ตรวจสอบรูปแบบก่อน
    is_valid, message = is_valid_userid(user_id)
    if not is_valid:
        st.error(message)
        return

    virtual_email = format_email(user_id)
    
    try:
        # เช็กในตาราง profiles ก่อนว่ามี ID หรือยัง (ป้องกัน ID ซ้ำ)
        check_id = supabase.table("profiles").select("id").eq("student_id", student_id).execute()
        if len(check_id.data) > 0:
            st.error("รหัสนักเรียนนี้ถูกใช้สมัครไปแล้ว!")
            return

        # ทำการสมัครสมาชิก
        res = supabase.auth.sign_up({"email": virtual_email, "password": password})
        
        if res.user:
            data = {
                "id": res.user.id,
                "full_name": full_name,
                "student_id": student_id,
                "role": "player" 
            }
            supabase.table("profiles").insert(data).execute()
            st.success(f"✅ สมัครสำเร็จ! ใช้ UserID: {user_id} ในการเข้าใช้งาน")
        else:
            st.error("UserID นี้ถูกใช้งานแล้ว กรุณาตั้งชื่ออื่น")
            
    except Exception as e:
        # ถ้า Supabase ฟ้องว่า Email ซ้ำ (ซึ่งก็คือ UserID ซ้ำ)
        if "already registered" in str(e).lower():
            st.error("❌ UserID นี้มีผู้ใช้งานแล้ว กรุณาใช้ชื่ออื่น")
        else:
            st.error(f"เกิดข้อผิดพลาด: {e}")

# --- ส่วนของหน้าจอสมัครสมาชิก (UI) ---
with tab_signup:
    st.info("💡 UserID: ภาษาอังกฤษ/ตัวเลข มากกว่า 6 ตัว และห้ามซ้ำกับคนอื่น")
    new_uid = st.text_input("ตั้ง UserID ที่ต้องการ", key="reg_uid")
    new_pass = st.text_input("ตั้งรหัสผ่าน (6 ตัวขึ้นไป)", type="password", key="reg_pass")
    full_name = st.text_input("ชื่อ-นามสกุล")
    student_id = st.text_input("รหัสนักเรียน")
    
    if st.button("สร้างบัญชีผู้เล่น", use_container_width=True):
        if new_uid and new_pass and full_name and student_id:
            sign_up(new_uid, new_pass, full_name, student_id)
        else:
            st.warning("กรุณากรอกข้อมูลให้ครบทุกช่อง")
